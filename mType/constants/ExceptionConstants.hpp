#pragma once

namespace constants {
    namespace exception {
        // Base exception class name that all exception classes must inherit from
        constexpr const char* BASE_EXCEPTION_CLASS = "Exception";
    }
}
